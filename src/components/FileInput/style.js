const style = (theme) => ({
  fileUpload: {
    display: 'inline-block',
    padding: '6px 12px',
    cursor: 'pointer',
    backgroundColor: '#4CAF50',
    color: '#fff',
    borderRadius: '4px',
  }

});
export default style;